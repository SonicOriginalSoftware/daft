{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Debug Portal Server",
      "type": "shell",
      "options": { "cwd": "${workspaceFolder}/portal/server" },
      "command": "cargo build",
      "problemMatcher": []
    }
  ]
}
